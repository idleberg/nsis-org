# @nsis/dent-spec

## 0.1.0

### Minor Changes

- e7eba33: Add `@nsis/dent-spec`, the Dent Style Specification: the normative prose in `SPEC.md`, the data
  tables every implementation generates its lookups from, JSON Schemas for those tables and for a
  case's `options.toml`, and 29 byte-exact conformance cases.
  
  The specification is the authority on Dent style. Where an implementation disagrees with it, the
  implementation is wrong, and rule changes land here first.
- 0ecfbac: first release 🎉
