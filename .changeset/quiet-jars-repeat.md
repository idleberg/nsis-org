---
"@nsis/dent": patch
---

fix indentation of `${While}`, `${EndUnless}` and `${MementoSectionEx}` blocks, treat `${Case_Else}` as a case arm, and normalise the casing of `${EndWhile}`, `${EndUnless}`, `${Next}`, `${Case2}`–`${Case5}`, `${Case_Else}` and `${MementoSectionEx}`
